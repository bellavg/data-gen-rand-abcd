# OpenABC-D: A Large-Scale Dataset For Machine Learning Guided Integrated Circuit Synthesis 
[![Version](https://img.shields.io/badge/Version-1.0.0-brightgreen)](https://github.com/NYU-MLDA/OpenABC) 
[![License](https://img.shields.io/badge/License-BSD%203--Clause-blue.svg)](https://opensource.org/licenses/BSD-3-Clause)
[![Github Repo stars](https://img.shields.io/github/stars/NYU-MLDA/OpenABC?style=social)]()
[![Github forks](https://img.shields.io/github/forks/NYU-MLDA/OpenABC?style=social)]()

## Overview

This repository contains the code necessary to generate OpenABC-D dataset and run models on OpenABC-D dataset.

To use the code in this repository, you'll need to download our dataset and extract it in the same directory as this repository, making sure it's name `OPENABC_DATASET`: <UV link>. Make sure everything all the zipped files in the subfolders therein are also unzipped.

### Dataset overview

**OpenABC-D** is a large-scale labeled dataset generated by synthesizing open source hardware IPs using state-of-art logic synthesis tool **yosys-abc**. We consider 29 open-source hardware IP designs collected from various sources (MIT-CEP, IWLS, OpenROAD, OpenPiton etc) and synthesized them with 1500 random synthesis flows (we call them *synthesis recipes*).

Each synthesis flow has a predefined length **L** (L=20, in our case). We preserved all AIGs: starting, intermediate and final AIGs with labels like number of nodes, longest path, sequence of atomic synthesis transofrmations (*rewrite*, *refactor*, *balance* etc.) along with graph statistics, area and delay of final AIG. 

We converted the AIGs in **pytorch** data format that can be directly used by a machine learning engineer lessening the effort of costly labeled data generation and pre-processing. **OpenABC-D** can be used for a variety of learning tasks on logic synthesis such as:

1. Predicting quality of result (QoR) performance of a *synthesis recipe* on a hardware IP. 
2. Area and delay prediction post techonolgy mapping.
3. Learn functional and structural features of AIG using self-supervised labels (useful for tasks like RL-based logic synthesis)

Our dataset can easily be used for graph-based machine learning framework like [Pytorch-Geometric](https://github.com/rusty1s/pytorch_geometric). The data generation pipeline of **OpenABC-D** is shown as follows:
![](https://github.com/NYU-MLDA/OpenABC/blob/master/figures/DatagenerationPipeline.png)

## Installing dependencies

This code uses Python 3.9. Our *requirements.txt* file is formatted to create an [Anaconda](https://www.anaconda.com/) environment with all the pre-requisites packages for running our framework and models. We recommend installing the packages using *requirements.txt* file provided in our repository. First install Anaconda (or at least [miniconda](https://docs.anaconda.com/miniconda/)) and then create an environment, for example: `conda create --name abcd-env --file requirements.txt`

Below is the list of packages with versions we used:

- cudatoolkit = 10.1 // not found on pypi, only in Anaconda
- numpy >= 1.20.1
- pandas >= 1.2.2
- pickleshare >= 0.7.5
- python >=3.9
- pytorch = 1.8.1
- scikit-learn = 0.24.1
- torch-geometric=1.7.0
- tqdm >= 4.56
- seaborn >= 0.11.1
- networkx >= 2.5
- joblib >= 1.1.0

Here are few resources to install the packages (if not using *requirements.txt*):

- [Pytorch](https://pytorch.org/get-started/locally/)
- [Torch-geometric](https://pytorch-geometric.readthedocs.io/en/latest/notes/installation.html)
- [Networkx](https://networkx.org/documentation/stable/install.html)

Make sure that that the `cudatoolkit` version in the gpu matches with the `pytorch-geometric`'s (and dependencies) CUDA version.

## Running our workflow

### 1. Data generation

Be aware that this can generate up to ~1.5 TB of data!

```
├── datagen
│   ├── automation                    # Scripts for automation (Bulk/parallel runs for synthesis, AIG2Graph conversions etc.)
│   │   ├── automate_bulkSynthesis.py         # Shell script for each design to perform 1500 synthesis runs
│   │   ├── automate_finalDataCollection.py   # Script file to collect graph statistics, area and delay of final AIG
│   │   ├── automate_synbench2Graphml.py      # Shell script file generation to involking andAIG2Graphml.py
│   │   └── automate_synthesisScriptGen.py    # Script to generate 1500 synthesis script customized for each design
│   └── utilities                     # Scripts for converting data and computing statistics
│       ├── andAIG2Graphml.py		          # Python utility to convert AIG BENCH file to graphml format
│       ├── collectAreaAndDelay.py            # Python utility to parse log and collect area and delay numbers
│       ├── collectGraphStatistics.py         # Python utility to for computing final AIG statistics
│       ├── pickleStatsForML.py               # Pickled file containing labels of all designs (to be used to assign labels in ML pipeline)
│       ├── PyGDataAIG.py		              # Python utility to convert synthesized graphml files to pytorch data format
│       └── synthID2SeqMapping.py	          # Python utility to annotate synthesis recipe using numerical encoding and dump in pickle form
```

The `automation` directory contains python scripts for automating bulk data generation (e.g. synthesis runs, graphml conversion, pytorch data generation etc.) and the `utilities` folder have utility scripts performing various tasks and are called from automation scripts.

To reproduce our data generation steps, first run the scripts in the `automation` directory:

1. *Step 1*: Run *automate_synthesisScriptGen.py* to generate customized synthesis script for 1500 synthesis recipes. You will need to provide two arguments: `HOME`, which is the top-level directory where you unzipped the dataset (it'll look for the `synScripts/` directory, make sure all `*.zip` files inside are unzipped) and `SCRIPT`, which is the path to a directory of reference scripts. Unzip `bench_openabcd/referenceScripts.zip` and pass the path to the unzipped folder as your `SCRIPT` argument.

```
python automate_synthesisScriptGen.py --home ~/Downloads/OPENABC_DATASET/ --script ../../bench_openabcd/referenceScripts/
```

2. *Step 2*: Run *automate_bultkSynthesis.py* to generate a shell script for a design, where HOME is the top-level directory where you unzipped the dataset (this script looks for the `bench/` sub-directory, make sure all `*.zip` files inside are unzipped). Run the shell script to perform the synthesis runs. Make sure **yosys-abc** is available in **PATH**.

```
python automate_bultkSynthesis.py --home ~/Downloads/OPENABC_DATASET/
```

3. *Step 3*: Run *automate_synbench2Graphml.py* to generate a shell script for generating graphml files, where HOME is the top-level directory where you unzipped the dataset. The shell script invokes *andAIG2Graphml.py* using 21 parallel threads processing data of each synthesis runs in sequence.

```
python automate_synbench2Graphml.py  --home ~/Downloads/OPENABC_DATASET/
```

Then in the `utilities` directory:

4. *Step 4*: Run *PyGDataAIG.py* to generate pytorch data for each graphml file of the format *designIP_synthesisID_stepID.pt*.

5. *Step 5*: Run *collectAreaAndDelay.py* and then *collectGraphStatistics.py* to collect information about final AIG's statistics.

6. *Step 6*: Run *pickleStatsForML.py* which will output *synthesisStatistics.pickle* file.

7. *Step 7*: Run *synthID2SeqMapping.py* utility to generate *synthID2Vec.pickle* file containing numerically encoded data of synthesis recipes.

### 2. Benchmarking models: Training and evaluation

The `models` directory contains the benchmarked model described in details in [our paper]. Essentially, all the SynthNet models are based on different GCN hidden layers and dimension, as well as recipe embedding used for the prediction models. See paper for more details

```
├── models
│   ├── classification
│   │   └── ClassNetV1
│   │       ├── model.py			 # Graph convolution network based architecture model
│   │       ├── netlistDataset.py                # Dataset loader
│   │       ├── train.py			 # Train and evaluation utility
│   │       └── utils.py			 # Utitility functions
│   └── qor
│       ├── SynthNetV1
│       │   ├── embedding.py
│       │   ├── evaluate.py
│       │   ├── model.py
│       │   ├── netlistDataset.py
│       │   ├── train.py
│       │   └── utils.py
│       ├── SynthNetV2
│       │   ├── evaluate.py
│       │   ├── model.py
│       │   ├── netlistDataset.py
│       │   ├── train.py
│       │   └── utils.py
│       └── SynthNetV3
│           ├── evaluate.py
│           ├── model.py
│           ├── netlistDataset.py
│           ├── train.py
│           └── utils.py
```

#### Case 1: Prediction QoR of a synthesis recipe

We recommend creating a following folder hierarchy before training/evaluating a model using our dataset and model codes:

	├── OPENABC-D
	│   ├── lp1
	│   │   ├── test_data_set1.csv
	│   │   ├── test_data_set2.csv
	│   │   ├── test_data_set3.csv
	│   │   ├── train_data_set1.csv
	│   │   ├── train_data_set2.csv
	│   │   └── train_data_set3.csv
	│   ├── lp2
	│   │   ├── test_data_set1.csv
	│   │   └── train_data_set1.csv
	│   ├── processed
	│   ├── synthesisStatistics.pickle
	│   └── synthID2Vec.pickle


```OPENABC-D``` is the top level directory containing the dataset, train-test split files, and labeled data available. Transfer all the relevant zipped pytorch data in the subdirectory ```processed```.

The user can now go the ```models``` directory and run codes for training and evaluation. An example run for **dataset split strategy 1** (Train on first 1000 synthesis recipe, predict QoR of next 500 recipe)

```
python train.py --datadir $HOME/OPENABC-D --rundir $HOMEDIR/NETV1_set1 --dataset set1 --lp 1 --lr 0.001 --epochs 60 --batch-size 32

```

Setting ```lp=1``` and ```dataset=set1``` will pick appropriate train-test split strategy dataset for QoR regression problem. The model will run for 60 epochs and report the training, validation and test performance on the dataset outputing appropriate plots.

Similarly for **split-strategy 2** and **3**, one can set the dataset as ```set2``` and ```set3``` respectively.

For evaluating performance of specific model on a custom curated dataset, a user can create appropriate CSV file with dataset instances and add it to dictionary entry in ```train.py```. For evaluating existing dataset split, one can run the following code.

```
python evaluate.py --datadir $HOME/OPENABC-D --rundir $HOMEDIR/NETV1_set1 --dataset set1 --lp 1 --model "gcn-epoch20-loss-0.813.pt" --batch-size 32

``` 

The test-MSE performance we obtained on our side are as follows:

|   Net Type |     Case-I     |   Case-II     |   Case-III   |
|    :---:   |     :---:      |    :---:      |    :---:     |
|    NetV1   |   0.648+-0.05  |  10.59+-2.78  |  0.588+-0.04 |
|    NetV2   |   0.815+-0.02  |  1.236+-0.15  |  0.538+-0.01 | 
|    NetV3   |   0.579+-0.02  |  1.470+-0.14  |  0.536+-0.03 |

### 3. Analysis

```
├── analysis
│   ├── findCommonTopKSynth.ipynb
│   ├── findCommonTopKSynth.py
│   ├── generateTSNEPlots.py
│   ├── synthesisAnalysis.ipynb
│   └── SynthesisVariation.ipynb
```

This folder folds both Python code and Jupyter notebooks used for analysis ...

<a id='Citation'></a>

### Additional files
```
├── bench_openabcd				# This contains the original bench files used to create the OpenABC-D dataset
├── bench_rtl					# This contains the original RTL files used to extract the combinational logic using Yosys and its corresponding synthesis setting
│   ├── src					# This contains the RTL verilog codes
│   └── synthesis_settings			# This contains the synthesis configuration used for running with OpenROAD
├── figures					# This contains the relevant figures describing the data generation pipeline as well as details of all the combinational logic extracted from benchmarks
├── leaf_level_verilog				# This is part of extended OpenABC-D dataset where all the leaf-level verilog modules have been generated as standalone RTL files present in Openroad.
│   ├── asap7
│   ├── gf12
│   ├── gf180
│   ├── gf55
│   ├── intel16
│   ├── intel22
│   ├── nangate45
│   ├── sky130hd
│   ├── sky130hd_fakestack
│   ├── sky130hs
│   └── tsmc65lp
```

## How to cite

If you use this code/dataset, please cite:

```
@misc{chowdhury2021openabcd,
      title={OpenABC-D: A Large-Scale Dataset For Machine Learning Guided Integrated Circuit Synthesis}, 
      author={Animesh Basak Chowdhury and Benjamin Tan and Ramesh Karri and Siddharth Garg},
      year={2021},
      eprint={2110.11292},
      archivePrefix={arXiv},
      primaryClass={cs.LG}
}
```
