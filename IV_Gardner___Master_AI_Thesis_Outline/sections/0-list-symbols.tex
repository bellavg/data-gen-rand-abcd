% \chapter{List of Symbols}
%         This list of notation can help the reader decipher the thesis:

%         \begin{align*}
%                 \mathbb R_+&, \mathbb R_+^*, \mathbb R_-^* & \text{set of real numbers $\geq 0, >0, <0$, respectively;} \\
%                 a & \in \mathbb R & \text{scalar containing the value of the Answer.}
%         \end{align*}

% TODO: AIG symbols: $G = (\mathcal{V}, \mathcal{E})$, $\operatorname{fi}$, $\operatorname{fo}$, $\prec$, $\tau$, $\mathrm{inv}$, $b_u$.
% TODO: structure symbols: $\ell(v)$, $\ell(G)$, $\mathcal{C}(v)$, $\mathcal{F}(v)$, $\operatorname{MFFC}(v)$, $\kappa(v)$, $\equiv$.
% TODO: task symbols: $S$, $S(G)$, $y(G)$, $f_\theta$.
% TODO: reduction symbols: $R$, $R(G)$, $\eta_{\mathcal{V}}$, $\eta_{\mathcal{E}}$, $\mu$, $w_{u'v'}$.
% TODO: encoder symbols: $h_v^{l}$, $\mathcal{N}(v)$, $e_{uv}$, $c_v^{(t)}$.

% \chapter{List of Abbreviations}
%         A list of acronyms could similarly be defined
%         \begin{table}[h]
%                 \centering
%                 \begin{tabular}{cl}
%                                 CNN & Convolutional neural network \\
%                                 CPU & Central processing unit \\
%                                 SBU & System Billing Unit \\
%                                 SGD & Student Gradient Descent
%                 \end{tabular}
%         \end{table}

% TODO: abbreviations in use: AIG, GNN, GCN, EDA, LS, ML, DAG, PI, PO, MFFC, QoR, PE, RWSE, RTL, FRAIG.
